# reactjs Start webpack example

This startup uses webpack and reactjs babel.

It has a basic navigation of index, about, and Contact Us i used instead of the router react component.
For simplicity.

---

To run it:

npm install
webpack

